 R←Test_025(stopFlag batchFlag);⎕TRAP
⍝ Exercise `Init`
⍝ Does not do anything but avoids "Init" being reported as uncovered by the tests
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←T._Failed

 ##.MakeHelpers.Init

 R←T._OK
⍝Done
