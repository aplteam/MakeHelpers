 R←Test_003(stopFlag batchFlag);⎕TRAP
⍝ Exercise YesOrNo
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←T._Failed

 :If batchFlag
     →0,R←T._NoBatchTest
 :EndIf

 0 0⍴⍞,0/⎕←'This test needed you: you will be asked a question; just press enter'

 {}1 ##.MakeHelpers.YesOrNo'Are you sure?'

 R←T._OK
⍝Done
